When you have a counter you can just create a FloatVariable to hold the value, for example the coin count, and the UI takes the SO reference and the coins itself adds 1 to the count when it's collected

Runtime set to have a set of enemies to display on the ui or clear the stage for example. Create a component which would automatically register the entity in the set and the UI will answer to any changes

Provide a list of behaviors or component to delete after player's death or etc.

Invincibility is not a property but a timer and when one component provides grants invincibility it must specify the time window and we choose the max between current time and provided

The behavior when target has ranged attack is to back up when player is near and to get close when player is far


