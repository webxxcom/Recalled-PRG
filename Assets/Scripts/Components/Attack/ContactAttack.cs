using UnityEngine;

[RequireComponent(typeof(Collider2D))]
public class ContactAttack : MonoBehaviour
{
    //TODO The contact attack for trap saw. God knows how to properly implement((
    //[field: SerializeField] public AttackData AttackData { get; private set; }

    //private void OnTriggerEnter2D(Collider2D collision)
    //{
    //    EntityController ec = collision.GetComponentInParent<EntityController>();

    //    if (ec)
    //    {
    //        ec.HealthComponent.Change(gameObject, AttackData.DealtDamage);
    //    }
    //}
}
